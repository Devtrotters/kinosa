const size = {
    s: '768px',
    m: '1440px',
    lg: '1920px'
};

const device = {
    s: `min-width: ${size.s}`,
    m: `min-width: ${size.m}`,
    lg: `min-width: ${size.lg}`
};

export default { size, device };
