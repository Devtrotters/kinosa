const size = {
    s: '768px',
    menu: '1024px',
    m: '1440px',
    lg: '1920px'
};

const device = {
    s: `min-width: ${size.s}`,
    menu: `min-width: ${size.menu}`,
    m: `min-width: ${size.m}`,
    lg: `min-width: ${size.lg}`
};

export default { size, device };
